16 | 缓存策略：如何使用缓存来减少磁盘IO？
    选择只读缓存还是读写缓存
      读写缓存这种设计模式，它是不可靠的，是一种牺牲数据一致性换取性能的设计
    kafka 如何保证可靠性
      通过不同节点副本保证数据可靠性，即使某一个节点出现问题，可以通过其他节点恢复


    保持数据新鲜
      每次同步，保持全量更新，如果数据比较大，可以通过增量更新
      定期更新
      设置过期时间

    置换策略
      LRU
