35-mysql调优值索引失效与优化
  1、覆盖索引，避免回表
  2、自增字段作为主键优化查询，顺序插入，避免分页
  3、前缀索引优化：只使用字段中前几个字符建立索引，有局限性，无法order by 无法使用前缀索引，也无法使用覆盖索引
  4、防止索引失效：
    hash 索引只有在=条件下才生效
    like %：无法使用最左匹配
    如果or 有一列没有使用索引，那么涉及的索引，都会失效
  5、对索引列进行函数操作，导致索引失效



