01-基础架构
  什么是关系型数据结构：一个用户id，对象一个用户属性集合，这样的结构叫做k-v数据库
  选择什么数据结构：k-v，他们的key，差别不大，重要事对value数据结构的选择
  可以对数多什么操作
    put
    get
    delete
    scan：根据key，获取某一个范围的数据
  持久化

  模块设计：
    访问模块
      socket
      动态链接库
    操作模块
    索引模块
    储存模块

  为什么时候hash作为索引
    redis 他是k-value类型，hash他的时间复杂度为O(1)，如果想用范围操作，可以通过redis的数据结构解决该问题
