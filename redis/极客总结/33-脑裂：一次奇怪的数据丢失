33-脑裂：一次奇怪的数据丢失
  指一个主从集群中有两个主节点，客户端不知道从哪里写
  确认问题：是不是数据同步出现数据丢失
  排查：通过对比复制进度差

  排查客户端日志：发生主从切换时候，客户端任然和 old 主节点通信，进而发生脑裂问题
  问题：只会造成新数据分布在不同的主库上，并不会造成数据丢失
  发现问题：主库假故障导致脑裂

  主库变为从库，通过新主库的RDB，导致数据丢失

  解决方法：
    min-slaves-to-write：主库能进行数据同步的最小从库数据量
    min-slaves-max-lag：从库给主库的AVK 最大延迟

