17-为什么cpu架构也会影响性能
  在NUMA出现远端访问，增加引用执行时间
  解决方法：绑定CPU
  优化出现问题：把网络中断的绑定在一个cpu
  出现问题：和redis 不在同一个核上
  解决方法：网络中断和cpu内核绑定在同一个内核上
  绑定cpu需要注意的问题：cpu编号问题

  绑定的缺点：redis的所有线程竞争切换
  解决方法：
    方案1）绑定物理cpu，不要绑定逻辑cpu
    方案2）优化redis源码
