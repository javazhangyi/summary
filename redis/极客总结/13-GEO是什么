13-GEO是什么
  基于进度和维度保存的信息
  原理：通过z-set，把经度和纬度保存为权值，通过geoHash 编码（二分区）

  定义自己的数据类型
    redisObject
      元数据类型：
        type：值类型：前面我们学的五种数据基本类型
        encoding：编码方式 表示基本的数据结构
        lru：记录最后访问的时间，用于淘汰策略
        refcount：记录对象引用

      ptr：指向数据指针 8字节
  步骤：
    定义新的数据类型底层结构
    增加redisObject
    开发新类型创建和释放函数
    开发新类型的命令


通过geohash，获取他的一个经度纬度的二进制编码，再通过这个二进制编码合成一个，把这个二进制编码合成一个整数，变为score，再通过scope 获取范围值