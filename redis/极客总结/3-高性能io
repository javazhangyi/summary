3-高性能io
  单线程：这里的单线程是指网络读写，以及键值操作是单线程的，他主要采用io多路复用，将感兴趣的事件注册到内核或只能怪，内核监听，当有事件回调
    被封装成事件处理器，进入事件队列，交给redis处理，避免多线程的并发控制问题

    在“Redis 基本 IO 模型”图中，你觉得还有哪些潜在的性能瓶颈吗？欢迎在留言区写下你的思考和答案，我们一起交流讨论。
        aof always刷盘：在主线线程
        big-key删除
        复杂的命令
        并发量非常大：读写是同步
        内存淘汰：在主线程
        大量的key过期：过期处理在主线程
        主从同步：生成fork瞬间也是听不的