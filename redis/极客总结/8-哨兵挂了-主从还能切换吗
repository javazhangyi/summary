8-哨兵挂l-主从还能切换吗
  哨兵原理
    基于pub/sub(订阅与发布)组成哨兵集群，可以可以知道其他哨兵信息
  哨兵如何知道其他从库信息
    通过向主库发送info命令获得从库信息
    获得信息，然后与其他哨兵建立简洁
  哨兵如何与客户端通信
    通过发布与订阅

  哨兵的频道
    主库下线
      +sdown：进入-主观下线
      -sdown：退出-主观下线
      +odown：进入-客观下线
      -odown：退出-客观下线
    从库重新分配事件：
      +slave-reconf-sent：哨兵发送slave重新配置主库
      +slave-reconf-ipprog：从库重新配置新主库，尚未同步
      +slave-reconf-done：从库配置新的主库，且同步完成
    新主库切换：+swait-master：新主库地址发生变化
  通过这些事件通知，监控主从切换的重要事件，了解切换进度

  哪一个哨兵执行主从切换：通过投票选择并（满足条件：拿到半数以上的票，并且大于配置哨兵的里面的配置）
    如果投票失败，则等待下一轮投票（隐藏的问题至少三个哨兵）

  哨兵关键机制
    通过主节点发布订阅，获取其他哨兵信息
    通过主节点INFO 获取从节点信息
    通过哨兵的发布与订阅，客户端获取哨兵之间的事件通知

