24-缓存异常：解决缓存与数据不一致问题
  缓存常见的问题
    雪崩：大面积key失效
    击穿：失效那一瞬间，大量的请求过来
    穿透：数据库也不存在
    数据不一致
      原因：
        删除缓存或者更新db失败，造成数据不一致：重试机制+延迟双删；任然会存在数据数据不一致的方案
        更新db或者缓存中，线程并发带来的数据不一致：先更新db再删缓存
