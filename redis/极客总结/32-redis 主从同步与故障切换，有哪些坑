32-redis 主从同步与故障切换，有哪些坑
  数据不一致
    网咯延迟
    从库阻塞
    解决方法：网络要好，监控同步进度

  读到过期数据
    惰性删除
    定期删除

    3.2 之前从库会放回过期数据，3.2之后，会返回空值
    问题：还是会读取到过期数据
      设置多少时间过期
      设置一个时间戳过期
    解决方法：直接设置过期时间戳

  配置不合理，导致服务挂掉
    protected-mode：是否可以被其他的机器的哨兵访问
    cluster-node-timeout：redis cluster 相应实例的心跳超时间
