29-无锁的原子操作：redis如何应对并发访问
  redis提供的方法：
    加锁和原子操作
    问题：
      加锁操作多，影响并发性能
      需要提供分布式锁

    原子性操作
      将多个操作变为一个操作：INCR/DECR
      使用lua脚本


注意点
    lua尽量使用通用的逻辑代码，避免把代码写死，使lua脚本的重用性更高
    通过script load 吧lua加载到redis中，得到一个摘要值，通过该摘要执行该脚本，避免网络开销