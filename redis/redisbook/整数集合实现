整数集合实现:数组-zet数据类型
  可以保存int16_t,int32_t,int64_t
  intset
    uint32_t encoding
    uint32_t length
    int8_t contents[]

  具体类型取决于encoding类型

  升级
    扩展新的空间
    交老数据转化为新的类型，并且保存顺序
    将新元素保存在底层数组
  时间复杂度为O(n)

  升级的好处
    提升灵活性：c语言是静态类型，int_32 只能放32，通过自动审计，不必担心类型错误

  节约内存
    只有需要的时候才会升级，而不是一开始，就分配超大内存

  降级
    整数集合不支持降级，审计后，就会一直保存升级状态





