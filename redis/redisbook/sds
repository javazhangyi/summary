sds
  int len:分配的长度
  int free：剩余长度
  char[] buf：存储空间

  与C 的区别
    C语言获得长度，需要遍历O(n)，SDS有属性存储O(1)

    缓冲区溢出
      C语言总中：s1,s2两个字符串空间相邻，如果突然对一个字符串添加，造成s2的值被修改
      SDS：自动扩容，在拼接前检查

    减少膝盖字符带来的内存分配次数
      C语言：每次重新操作都要重新分配内存
      SDS：空间预分配和惰性释放解决这个问题
        空间预分配：小于1M，每次分配就是他的2倍，大于1M，多分配1M
        惰性释放L通过free字段来擦除数据，保留给以后使用

    二进制安全
      C语言必须是某种编码，且字符串不能有空白字符，限制他只能保存文本，不能保存其他二进制文件
      SDS没有限制

    兼容部分C字符串函数
      SDS 末尾是\0,可以重用部分字符串函数


总结
  是否可以获取字符串长度
  是否可以造成缓冲区溢出，或者是否有自动扩容
  是否多次分配内存
  是否二进制安全
  兼容兼容所有字符函数
