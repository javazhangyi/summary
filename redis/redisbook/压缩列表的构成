压缩列表的构成
  引用场景：为节约内存而开发，有一个类特殊编码的连续内存组成的顺序数据结构

  zlbytes
  zltail
  zllen
  entryX
  zlend

  entryX：每一个节点可以保存一个字节数组或者一个整数值
    字节数组的选择
      长度小于2^6-1的字节数组
      长度小于2^14-1字节数组
      长度小于2^32-1的字节数组

    整数类型选择
      4位长介于0~12的无符号整数
      1字节长有符号整数
      3字节长有符号整数
      int16_t整数
      int32_t
      int64_t
    entryX
      previous_entry_length：记录前一个字节长度
      encoding
      content:类型


  连锁更新
    添加或者删除节点导致连续更新，（资源恰好在瓶颈，添加后，想多迷路骨牌，依次扩容）

    但是造成的问题的概率很低
      必须是阀值连续，而且是大范围的
